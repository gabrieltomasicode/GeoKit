# GeochemLOD-Processor
Pipeline Python (Pandas/Regex) para tratamento de dados geoquímicos. Aplica imputação dinâmica em valores censurados (&lt;LOD) colunas com descarta e>30% de censura. | Python pipeline (Pandas/Regex) for geochemical data processing. Applies dynamic imputation on censored values (&lt;LOD) and drops columns with >30% censorship.
